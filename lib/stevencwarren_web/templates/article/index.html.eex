<section class="section">
  <h1 class="title">Articles that I need to read</h1>
  <div class="columns">
    <div class="column">
      <h2 class="subtitle">Recents</h2>
      <ol>
        <%= for article <- @recent_articles do %>
          <li><a href="<%= article.url %>" target="_blank"><%= article.url %></a> - <%= article.category.name  %></li>
        <% end %>
      </ol>
    </div>

    <div class="column is-one-quarter">
      <div class="columns is-multiline">
        <div class="column is-full">
          <h2 class="subtitle">Categories</h2>
          <ul>
            <%= for category <- @categories do %>
              <li><a href="<%= StevencwarrenWeb.Router.Helpers.category_path(@conn, :show, category.slug) %>"><%= category.name %></a></li>
            <% end %>
          </ul>
        </div>

        <%= if @user do %>
          <div class="column is-full">
            <a class="button is-link" href="<%= StevencwarrenWeb.Router.Helpers.article_path(@conn, :new) %>">Add an Article</a>
          </div>
        <% end %>
      </div>
    </div>
  </div>
</section>
